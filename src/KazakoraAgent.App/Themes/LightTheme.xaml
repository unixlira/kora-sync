<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=System.Runtime"
                    xmlns:imaging="clr-namespace:System.Windows.Media.Imaging;assembly=PresentationCore">

    <Color x:Key="WindowBackgroundColor">#F3F4F7</Color>
    <Color x:Key="CardBackgroundColor">#FFFFFF</Color>
    <Color x:Key="CardBackgroundHoverColor">#F7F8FA</Color>
    <Color x:Key="BorderColor">#E4E5EC</Color>
    <Color x:Key="TextPrimaryColor">#12131A</Color>
    <Color x:Key="TextSecondaryColor">#6B6D80</Color>

    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{StaticResource WindowBackgroundColor}" />
    <SolidColorBrush x:Key="CardBackgroundBrush" Color="{StaticResource CardBackgroundColor}" />
    <SolidColorBrush x:Key="CardBackgroundHoverBrush" Color="{StaticResource CardBackgroundHoverColor}" />
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}" />
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}" />

    <!-- Sombra mais presente no claro — sem ela os cards brancos se perdem no fundo claro. -->
    <sys:Double x:Key="CardShadowOpacity">0.10</sys:Double>
    <sys:Double x:Key="CardShadowDepth">2</sys:Double>

    <!-- Versão do logo com cores que ficam legíveis sobre fundo claro. -->
    <imaging:BitmapImage x:Key="LogoImageSource" UriSource="/Assets/logo_light.png" />

    <!-- Tamanho original — só o do tema escuro ficou 10px maior, pedido explícito 2026-08-04. -->
    <sys:Double x:Key="LogoHeight">94</sys:Double>

</ResourceDictionary>
